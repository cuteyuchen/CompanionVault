# 贡献指南

1. 使用 `companion-vault new-persona <id> --name <名称>` 创建目录。
2. 至少填写 3 个可观察核心特征、1 个缺点、3 个独特行为特征。
3. 提供不少于 10 个回复示例，覆盖日常、开心、倾诉、建议、分歧、修复、重逢和边界。
4. 不复制影视、动漫或现实人物的大段受版权保护内容。
5. 不设计排他依赖、情绪勒索或虚构现实记忆。
6. 运行 `companion-vault validate` 与 `pytest`。
